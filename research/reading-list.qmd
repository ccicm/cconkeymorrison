---
title: ""
page-layout: full
engine: markdown
---

::: page-header
# Reading List
:::

Papers I've been reading — collaborator work, methodological references, and things worth knowing about. Updated as I go.

------------------------------------------------------------------------

```{=html}
<div id="reading-list"><p class="pub-loading">Loading…</p></div>

<script>
(async function() {
  const container = document.getElementById('reading-list');

  function getBookSvg() {
    const c = (window.CCM_COLORS && window.CCM_COLORS.accent) || '#c8922a';
    return `<svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="${c}" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round"><path d="M2 3h6a4 4 0 0 1 4 4v14a3 3 0 0 0-3-3H2z"/><path d="M22 3h-6a4 4 0 0 0-4 4v14a3 3 0 0 1 3-3h7z"/></svg>`;
  }

  let entries = [];
  try {
    const res = await fetch('/data/reading.json');
    if (!res.ok) throw new Error();
    entries = await res.json();
  } catch {
    container.innerHTML = '<p style="color:var(--text-muted);font-size:0.9rem;">Could not load reading list.</p>';
    return;
  }

  if (!entries.length) {
    container.innerHTML = '<p style="color:var(--text-muted);font-size:0.9rem;">Nothing here yet.</p>';
    return;
  }

  async function fetchMeta(doi) {
    try {
      const res = await fetch(`https://api.crossref.org/works/${encodeURIComponent(doi)}?mailto=connorconkeymorrison@gmail.com`);
      if (!res.ok) return null;
      const data = await res.json();
      const w = data.message;
      const authors = (w.author || []).slice(0, 5).map(a => {
        const last = a.family || '';
        const init = a.given ? a.given[0] + '.' : '';
        return `${last}, ${init}`.trim().replace(/,$/, '');
      }).join(', ') + ((w.author || []).length > 5 ? ', et al.' : '');
      return {
        title: w.title?.[0] || 'Untitled',
        journal: w['container-title']?.[0] || '',
        year: w.published?.['date-parts']?.[0]?.[0] || '',
        authors,
        url: `https://doi.org/${doi}`,
      };
    } catch { return null; }
  }

  const metas = await Promise.all(entries.map(e => fetchMeta(e.doi)));

  const html = entries.map((e, i) => {
    const meta = metas[i];
    const url = meta ? meta.url : `https://doi.org/${e.doi}`;
    const title = meta ? meta.title : e.doi;
    const journal = meta ? [meta.authors, meta.year, meta.journal].filter(Boolean).join(' · ') : '';
    return `<div class="pub-entry-block">
      <div class="pub-favicon-fallback">${getBookSvg()}</div>
      <div class="pub-entry-text">
        <a href="${url}" target="_blank" class="pub-title-link">${title}</a>
        ${journal ? `<div style="font-size:0.82rem;color:var(--text-muted);margin-top:0.2rem;font-style:italic;">${journal}</div>` : ''}
        ${e.note ? `<div style="font-size:0.85rem;color:var(--text-muted);margin-top:0.35rem;line-height:1.55;">${e.note}</div>` : ''}
      </div>
    </div>`;
  }).join('');

  container.innerHTML = html;
})();
</script>
```

------------------------------------------------------------------------

::: about-section
To add a paper: edit `data/reading.json` with the DOI and a short note. Metadata loads automatically from Crossref.
:::
